#ifndef SUDOKURA_PLATFORM_FONTS_H
#define SUDOKURA_PLATFORM_FONTS_H

#include <limits.h>

const char *find_font_path_dynamic(char out[PATH_MAX], const char *cli_override);

#endif /* SUDOKURA_PLATFORM_FONTS_H */
